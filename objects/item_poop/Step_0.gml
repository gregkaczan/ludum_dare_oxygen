/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if !is_collected {
if !is_dragged {

	if !position_meeting(x,y,obj_blocker_poop){  motion_add(point_direction(x,y,obj_blocker_poop.x,obj_blocker_poop.y),1) }
		else motion_set(0,0);

}
}